// Package formcmd provides the "form" parent command, which groups interactive,
// TUI-based subcommands for issue creation and modification. Form commands
// prompt the user interactively for field values and produce human-readable
// output only — they are not suitable for agent automation.
package formcmd
