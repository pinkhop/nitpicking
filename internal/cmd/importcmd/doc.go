// Package importcmd provides the "import" parent command, which groups
// subcommands for importing issues from structured file formats (currently
// JSONL). The parent command has no action of its own — it exists only to
// namespace the import subcommands.
package importcmd
