// Package version implements the "version" command, which prints the
// application's build version and — when --verbose is set — VCS metadata
// (version control system, commit revision, and commit timestamp). Output
// format is selectable via --format: human-readable text (default) or JSON.
package version
