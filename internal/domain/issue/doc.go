// Package issue defines the core issue domain model: issue IDs, roles,
// priorities, facets, state machines, the issue entity itself, and all
// associated business rules (parent constraints, readiness, completion
// derivation, soft deletion). This package has no dependencies on persistence
// or CLI concerns.
package issue
