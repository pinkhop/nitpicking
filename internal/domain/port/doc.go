// Package port defines the driven port interfaces — the contracts that the
// domain requires from its persistence layer. The storage adapter (SQLite)
// implements these interfaces; the application layer depends only on the
// abstractions.
package port
