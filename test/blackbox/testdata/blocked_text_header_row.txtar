# Verify that blocked text output includes the new default all-caps header row.

np-init BHDR
np-seed task 'Blocker task' alice BLOCKER
np-seed task 'Blocked task' alice BLOCKED
np-seed-rel BLOCKED blocked_by BLOCKER alice

exec np blocked
stdout '^ID\s+PRIORITY\s+ROLE\s+STATE\s+TITLE'
