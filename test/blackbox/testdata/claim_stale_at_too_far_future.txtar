# Verify that --stale-at rejects timestamps more than 24h in the future.

np-init TF
np-seed task 'Too far task' tf-author TASK

! exec np claim $TASK --author tf-author --stale-at 2099-01-01T00:00:00Z
stderr '24h'
