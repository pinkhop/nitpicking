# Verify that description round-trips through json create and show.

np-init FLAG
stdin input.json
exec np json create --author flag-agent
np-capture TASK id

exec np show $TASK --json
np-json-has description 'Detailed explanation of the task'

-- input.json --
{"role":"task","title":"With description","description":"Detailed explanation of the task"}
