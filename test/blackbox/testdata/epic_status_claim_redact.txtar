# Epic status JSON output does not expose claim_id for claimed children.

np-init ER
np-seed epic 'Redaction audit epic' redact-epic-agent EPIC
np-seed task 'Claimed child' redact-epic-agent CHILD --parent $EPIC --claim

exec np epic status --json
! stdout '"claim_id"'
