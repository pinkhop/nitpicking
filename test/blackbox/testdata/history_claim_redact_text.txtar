# Verify that the text output of issue history does not leak the claim ID
# (bearer token). Claiming no longer produces a history event; the history
# only records "created" for a newly seeded issue.

np-init HC
np-seed task 'History claim redact' hist-agent TASK --claim

exec np issue history $TASK
stdout 'created'
! stdout $TASK_CLAIM
