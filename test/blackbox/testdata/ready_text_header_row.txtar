# Verify that ready text output includes the new default all-caps header row.

np-init RHDR
np-seed task 'Ready header task' alice TASK_A

exec np ready
stdout '^ID\s+PRIORITY\s+ROLE\s+STATE\s+TITLE'
