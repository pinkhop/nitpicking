# Verify that 'np issue search --json' produces valid JSON.

np-init IS
np-seed task 'Search subcommand audit' search-agent TASK

exec np issue search audit --json
! np-json-array-len issues 0
stdout '"created_at"'
