# Update with comment field is rejected — comments should be added via
# np json comment instead.

np-init UFLAG
np-seed task 'Comment test' flag-agent TASK --claim

stdin update.json
! exec np json update --claim $TASK_CLAIM
stderr 'unknown field'

-- update.json --
{"comment": "Progress update: halfway done"}
