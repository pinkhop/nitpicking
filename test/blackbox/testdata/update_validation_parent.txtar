# Update with a nonexistent parent exits with not-found error.

np-init UVAL
np-seed task 'Parent validation' val-agent TASK --claim

stdin update.json
! exec np json update --claim $TASK_CLAIM

-- update.json --
{"parent": "UVAL-zzzzz"}
