# Create with --claim flag: atomic create-and-claim in a single command.
# The response includes a claim_id and the issue is open with secondary_state claimed.

np-init WF
np-seed task 'Urgent hotfix' flag-agent TASK --claim

exec np show $TASK --json
np-json-has state open
np-json-has secondary_state claimed
np-json-has claim_author flag-agent
