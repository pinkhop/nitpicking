# Verify that --workspace flag with a valid workspace directory succeeds.
# The flag points at $WORK, which has a .np/ directory from np-init.

np-init WS

exec np --workspace $WORK list --json
np-json-array-len items 0
